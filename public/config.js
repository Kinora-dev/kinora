// Runtime config. Edit this file on your static host - no rebuild needed.
// Point baseUrl at the host serving manifest.json + reports/.
// Leave baseUrl empty in dev to use built-in mock data.
window.__PLAYBACK__ = {
  baseUrl: '', // e.g. 'https://reports.example.com'
  title: 'Playback',
}
